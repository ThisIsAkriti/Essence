{
    "installCommand":"npm install --legacy-peer-deps",
    "version":2,
    "builds":[
        {
            "src":"app.js",
            "use":"@vercel/node"
        },
        {
            "src":"app.js",
            "use":"@vercel/static"
        }
    ],
    "routes":[
        {"src":"/(.*)", "dest":"/"}
    ]
}